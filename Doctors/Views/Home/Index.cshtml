@{
    ViewData["Title"] = "Home";
}

<div class="container mt-4">
    <div class="d-flex justify-content-end mb-3">
        <span class="badge bg-info me-2">Total Users: @ViewBag.TotalUsers</span>
        <span class="badge bg-success">Logged In Users: @ViewBag.LoggedInUsers</span>
    </div>

    <h2 class="mb-4">Welcome to Doctors Forum</h2>
    <p>This is the Index page.</p>
</div>
